# tutorial0x01-simple-variables

来源提交：`ad3e78c Simple 4 variables`

本章介绍 Lua 的基础变量类型和 `type()` 用法。

## 内容

- `nil`、`number`、`string`、`boolean`
- `function`、`table`、`userdata`、`thread` 等复杂类型概念
- 使用 `type()` 获取变量类型
- 未声明变量默认得到 `nil`

## 运行

```bash
lua variable.lua
```
