# tutorial0x02-string

来源提交：`cd8905f string`

本章介绍 Lua 字符串的基本操作。

## 内容

- 使用 `#` 获取字符串长度
- 转义字符和多行字符串
- 使用 `..` 拼接字符串
- `string.format` 占位格式化
- `tostring` 类型转换
- `string.upper`、`string.find`、`string.gsub`
- `string.byte` 和 `string.char`

## 运行

```bash
lua StringOperation.lua
```
