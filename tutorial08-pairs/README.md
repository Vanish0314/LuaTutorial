# tutorial0x08-pairs

来源提交：`72393a6 pair`

本章介绍 `ipairs` 和 `pairs` 的差异。

## 内容

- `ipairs` 从 1 开始按连续整数索引遍历
- `ipairs` 遇到第一个 `nil` 停止
- `pairs` 遍历 table 中所有存在的键
- `pairs` 的遍历顺序不保证稳定
- table 构造器中显式 key 和无 key 项的区别

## 运行

```bash
lua pair.lua
```
