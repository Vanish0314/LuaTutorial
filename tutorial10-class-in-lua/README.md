# tutorial0x10-class-in-lua

来源提交：`123e7f4 class in lua`

本章介绍 Lua 中模拟对象的基础写法。

## 内容

- 使用 table 组织成员变量和成员函数
- 显式传入对象自身作为第一个参数
- 使用 `:` 调用方法
- `self` 作为默认传入的第一个参数
- `.` 调用和 `:` 调用的区别

## 运行

```bash
lua class.lua
```
