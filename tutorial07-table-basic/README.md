# tutorial0x07-table-basic

来源提交：`498f87c table basic`

本章介绍 Lua table 的数组用法。

## 内容

- 创建空 table 和带值 table
- Lua 数组索引默认从 1 开始
- 使用 `#` 获取数组长度时遇到 `nil` 的坑
- 二维数组的表现形式
- 使用 `for` 和 `ipairs` 遍历数组
- 自定义索引

## 运行

```bash
lua Table.lua
```
