# tutorial0x09-dictionary

来源提交：`0b14023 dictionary`

本章介绍把 Lua table 当作字典使用。

## 内容

- 使用 `dic.key` 写入字段
- 使用 `dic["key"]` 写入字段
- 使用 `pairs` 遍历键值
- 将字段赋值为 `nil` 来删除键

## 运行

```bash
lua dictionary.lua
```
